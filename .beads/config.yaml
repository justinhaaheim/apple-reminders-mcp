# Beads Project Configuration
issue_prefix: apple-reminders-mcp
default_priority: 2
default_type: task

# Sync behavior
sync:
  auto_import: true
  auto_flush: true

sync.remote: "git+http://local_proxy@127.0.0.1:44680/git/justinhaaheim/apple-reminders-mcp"